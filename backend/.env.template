# PAAPI Credentials & Configuration - Replace with your actual credentials
AMAZON_PAAPI_ACCESS_KEY="YOUR_ACCESS_KEY_HERE"
AMAZON_PAAPI_SECRET_KEY="YOUR_SECRET_KEY_HERE"
AMAZON_PAAPI_PARTNER_TAG="YOUR_PARTNER_TAG_HERE" # e.g., yourstoreid-20
AMAZON_PAAPI_HOST="webservices.amazon.sa" # Or your region's PAAPI host e.g. webservices.amazon.com for US, webservices.amazon.co.uk for UK
AMAZON_PAAPI_REGION="me-south-1" # Or your region e.g. us-east-1 for US, eu-west-1 for UK

# OpenAI API Key - Replace with your actual key
OPENAI_API_KEY="YOUR_OPENAI_KEY_HERE"

# Optional: Uvicorn Log Level
# LOG_LEVEL="info"

# Optional: Database URL (if used)
# DATABASE_URL="postgresql://user:password@host:port/database"

# --- SP-API Credentials & Configuration (Placeholders) ---
# These are highly sensitive and require a secure way to manage, especially the refresh token.
# LWA (Login with Amazon) Credentials for your SP-API Application
SPAPI_LWA_APP_ID="YOUR_LWA_APP_ID"
SPAPI_LWA_CLIENT_SECRET="YOUR_LWA_CLIENT_SECRET"
# The LWA Refresh Token for your application to access the seller's account.
# This token needs to be obtained via an OAuth2 authorization flow and stored securely.
# It also needs to be refreshed periodically.
SPAPI_LWA_REFRESH_TOKEN="YOUR_LWA_REFRESH_TOKEN_FOR_SELLER"

# AWS IAM User Credentials (for signing SP-API requests if not using an IAM role that grants SP-API execute-api)
# If using an IAM Role for SP-API access (recommended), these might not be directly used by the SDK
# if it assumes the role automatically (e.g., when running on EC2 with an instance profile).
# However, some SDKs or manual request signing might require them.
SPAPI_AWS_ACCESS_KEY_ID="YOUR_SPAPI_IAM_USER_ACCESS_KEY_ID" 
SPAPI_AWS_SECRET_ACCESS_KEY="YOUR_SPAPI_IAM_USER_SECRET_ACCESS_KEY"

# IAM Role ARN (if your SP-API application uses an IAM role to access SP-API)
# This is often the case for secure, delegated access.
SPAPI_ROLE_ARN="arn:aws:iam::ACCOUNT_ID:role/YOUR_SPAPI_EXECUTION_ROLE"

# SP-API Endpoint Configuration
# The endpoint depends on the region of the marketplace.
# KSA is in the EU region for SP-API: https://developer-docs.amazon.com/sp-api/docs/marketplace-ids
SPAPI_ENDPOINT="https://sellingpartnerapi-eu.amazon.com" # For EU region (includes SA)
SPAPI_REGION="eu-west-1" # AWS region for the EU SP-API endpoint.
